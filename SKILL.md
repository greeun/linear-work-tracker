---
name: linear-work-tracker
description: Automatic Linear issue tracking and updates for development work. Logs feature creation, bug fixes, code modifications, and deletions to Linear issues. Use when creating features, fixing bugs, modifying code, completing tasks, or when user mentions Linear, issue tracking, task update, work log, or progress tracking.
---

# Linear Work Tracker

Automatically track and log development work to Linear issues.

## Workflow Overview

```
작업 시작 → Linear 이슈 확인/생성 → 작업 수행 → 진행상황 기록 → 완료 시 상태 업데이트
```

## Decision Tree

1. **작업 유형 판단:**
   - **새 기능 개발?** → Feature 워크플로우
   - **버그 수정?** → Bug Fix 워크플로우
   - **코드 수정/리팩토링?** → Modification 워크플로우
   - **코드 삭제?** → Deletion 워크플로우

2. **Linear 이슈 상태:**
   - **이슈 ID 제공됨?** → 해당 이슈에 연결
   - **이슈 없음?** → 새 이슈 생성 또는 검색

## Feature 워크플로우 (새 기능)

### Step 1: 이슈 확인/생성

```bash
# 기존 이슈 검색
linear issue list --filter "title:기능명"

# 새 이슈 생성 (필요시)
linear issue create --title "기능명" --team "팀ID" --status "In Progress"
```

### Step 2: 작업 기록 시작

이슈에 작업 시작 코멘트 추가:
```
🚀 작업 시작
- 목표: [기능 설명]
- 예상 변경 파일: [파일 목록]
- 시작 시간: [현재 시간]
```

### Step 3: 진행상황 업데이트

주요 마일스톤마다 코멘트 추가:
```
📝 진행상황 업데이트
- 완료: [완료된 항목]
- 진행중: [현재 작업]
- 변경된 파일: [파일 목록]
```

### Step 4: 완료 처리

```bash
# 상태 업데이트
linear issue update [ISSUE_ID] --status "Done"

# 완료 코멘트
```
```
✅ 작업 완료
- 변경 요약: [요약]
- 변경된 파일: [파일 목록]
- 커밋: [커밋 해시]
```

## Bug Fix 워크플로우

### Step 1: 버그 이슈 연결

```bash
# 버그 이슈 검색
linear issue list --filter "type:bug title:버그명"
```

### Step 2: 디버깅 기록

```
🔍 버그 분석
- 증상: [버그 현상]
- 원인: [근본 원인]
- 영향 범위: [영향받는 기능]
```

### Step 3: 수정 및 검증

```
🔧 수정 완료
- 수정 내용: [변경사항]
- 테스트 결과: [검증 내용]
- 회귀 테스트: [결과]
```

## Modification 워크플로우 (리팩토링/수정)

### 기록 템플릿

```
🔄 코드 수정
- 수정 유형: [리팩토링/성능개선/코드정리]
- 대상: [파일/함수/클래스]
- 변경 전: [이전 상태 요약]
- 변경 후: [변경 후 상태]
- 이유: [수정 사유]
```

## Deletion 워크플로우 (삭제)

### 기록 템플릿

```
🗑️ 코드 삭제
- 삭제 대상: [파일/함수/클래스]
- 삭제 이유: [사유]
- 영향 범위: [의존성 확인 결과]
- 대체 방안: [있다면 설명]
```

## Linear API 사용

### 환경 설정

```bash
# API 키 설정 (환경변수)
export LINEAR_API_KEY="lin_api_xxxxx"
```

### 스크립트 사용

```bash
# 이슈 생성
python scripts/create_issue.py --title "제목" --description "설명"

# 코멘트 추가
python scripts/add_comment.py --issue-id "ISSUE-123" --body "내용"

# 상태 업데이트
python scripts/update_status.py --issue-id "ISSUE-123" --status "done"
```

상세 API 사용법은 [references/linear-api.md](references/linear-api.md) 참조.

## 자동 기록 규칙

### 항상 기록할 항목

| 작업 유형 | 필수 기록 |
|----------|----------|
| 파일 생성 | 파일명, 목적, 주요 함수/클래스 |
| 파일 수정 | 변경 전후 요약, 수정 이유 |
| 파일 삭제 | 삭제 이유, 의존성 확인 |
| 버그 수정 | 원인, 해결책, 테스트 결과 |

### 코멘트 형식

모든 코멘트는 이모지 prefix로 작업 유형 표시:
- 🚀 작업 시작
- 📝 진행상황
- ✅ 완료
- 🔍 분석/조사
- 🔧 수정/수리
- 🗑️ 삭제
- ⚠️ 주의/이슈
- 🔄 리팩토링

## Resources

- **Linear API 가이드**: [references/linear-api.md](references/linear-api.md)
- **이슈 생성 스크립트**: [scripts/create_issue.py](scripts/create_issue.py)
- **코멘트 추가 스크립트**: [scripts/add_comment.py](scripts/add_comment.py)
- **상태 업데이트 스크립트**: [scripts/update_status.py](scripts/update_status.py)
